# simple-Port-Scanner

A lightweight Bash-based TCP port scanner. Quickly checks a list of common ports to see which ones are open on a target host.

---

## 📌 Usage

```bash
chmod +x portscan.sh
./portscan.sh <target IP or domain>

Example

./portscan.sh 192.168.1.1

